8. 🔌 Контракт API (логика взаимодействия фронт ↔ бэк)
   Ниже — то, как это должно быть организовано логически (даже если названия параметров отличаются, смысл сохраняется). 

8.1 📄 Получение списка
Метод/эндпоинт: **GET /api/v1/feedback**

🔎 Query-параметры (типовой набор для UI):

* **page, page_size** — пагинация
* **q** — поиск
* **course_id** — фильтр по курсу
* **rating** или **rating_min/rating_max** — фильтр по оценке
* **show_hidden** или **is_hidden** — показывать/фильтровать скрытые
* **sort** (например, **created_at_desc**) — сортировка

📦 Ответ (структура):

* **items**: массив записей (для таблицы)
* **total**: общее количество записей под фильтры (для серверной пагинации)

8.2 📊 Метрики
Вариант A (рекомендованный): отдельный эндпоинт агрегатов, чтобы не мешать таблицу и статистику:

* **GET /api/v1/feedback/metrics** (с теми же фильтрами)

Пример ответа метрик:

* shown_total
* avg_rating
* hidden_total
* last_7_days 

Вариант B: возвращать метрики в ответе списка (как meta), но следить за кэшированием/нагрузкой. 

8.3 ➕ Создание
Метод/эндпоинт: **POST /api/v1/feedback**
Тело запроса (примерно): student_id, course_id, lesson_id, rating, comment, is_hidden. 

8.4 ✏️ Обновление
Метод/эндпоинт: **PATCH /api/v1/feedback/{id}**
Тело запроса (частично): rating, comment, is_hidden. 

8.5 🔐 Авторизация запросов
Все запросы раздела должны уходить с заголовком:
**Authorization: Bearer <JWT>** 
Это соответствует стандартной схеме Bearer-токена через Authorization header. ([IETF Datatracker][1])

---

9. 🗄️ Модель данных (что хранится)
   Минимально необходимые поля для таблицы отзывов:

* id
* student_id (FK на users)
* lesson_id (FK на lessons)
* rating (float)
* comment (text)
* is_hidden (bool)
* created_at (datetime) 

⚡ Производительность (индексы):

* (lesson_id), (student_id), (created_at), (is_hidden), возможно (rating) 

🔎 Поиск по comment:

* либо ILIKE (дороже),
* либо полнотекстовый индекс PostgreSQL (когда реально понадобится). 
  Для полнотекста обычно используют tsvector + GIN/GiST индексы. ([PostgreSQL][2])

🧩 Частое бизнес-правило:

* уникальность (student_id, lesson_id) — “один отзыв на урок от одного студента”, иначе дубли ломают метрики. 

---

10. ✅ Проверка работоспособности (чеклист)
    🖥️ Открыть экран фидбэка:

* таблица загрузилась
* метрики корректны
* кнопки «Обновить/Создать» активны 

🧪 Быстрые проверки поведения:

* Поиск: ввод части имени → количество записей меняется
* Фильтр по курсу: выбор курса → меняются таблица и метрики
* Фильтр по оценке: «Только 5» → остаются только максимальные оценки
* Сортировка: «Сначала новые» меняет порядок
* Пагинация: переход на другую страницу не ломает фильтры 

🛠️ CRUD-сценарии:

* Создание: открыть модалку, заполнить обязательные поля, сохранить → новая запись в таблице
* Редактирование: изменить оценку/комментарий/hidden, сохранить → строка обновилась
* Скрытые: включить «Показывать скрытые» → скрытые появляются/прячутся

---

📤 Экспорт данных
Раздел «Экспорт» позволяет выгрузить накопленные данные о посещаемости в файл для дальнейшего анализа или отчетности. Поддерживаются форматы **CSV** (например, для Excel) и **JSON** (для программной обработки). Перед экспортом можно отфильтровать записи и увидеть превью выгрузки прямо в интерфейсе. 

🧭 Сценарий экспорта
Обычный процесс:

1. Открыть раздел «Экспорт» (форма фильтров + выбор формата).
2. Указать фильтры: минимум курс; дополнительно — урок, студент, диапазон дат; выбрать CSV или JSON.
3. Нажать «Сформировать» → запрос к серверу, на странице появляется превью (первые N строк + сколько всего будет выгружено).
4. Нажать «Скачать» → браузер начинает загрузку файла (имя формируется автоматически).
5. (Опционально) «Очистить» → сброс фильтров и превью.

🧾 Форматы файлов

* CSV: текстовый файл с разделителями, первая строка — заголовки, далее по строке на запись. 
* JSON: массив объектов (каждая запись посещаемости — отдельный объект). 

⚙️ Техническая реализация экспорта
Вариант 1 (сервер формирует файл и отдаёт как вложение): FastAPI обычно возвращает файл через **FileResponse** или **StreamingResponse** (удобно для больших объёмов, чтобы не грузить всё в память).  ([FastAPI][3])

Вариант 2 (формирование на клиенте): сервер отдаёт JSON, фронт делает CSV/JSON локально и инициирует скачивание (например, через Blob и URL.createObjectURL). 

🔐 Разграничение доступа
Экспорт почти всегда защищён авторизацией и ролями:

* Admin — любые курсы/студенты
* Teacher — только свои курсы/занятия
* Student — (если предусмотрено) только свои данные
  Проверки должны быть и в UI, и на сервере.

---

📊 Аналитика (метрики и прогнозы)
Раздел «Аналитика» — центр отслеживания ключевых показателей успеваемости и посещаемости. Здесь данные из разных модулей объединяются, чтобы показать сводные метрики, тренды и прогнозы (включая прогноз риска пропуска следующего занятия на основе ML-модели).

[1]: https://datatracker.ietf.org/doc/html/rfc6750?utm_source=chatgpt.com "RFC 6750 - The OAuth 2.0 Authorization Framework"
[2]: https://www.postgresql.org/docs/current/textsearch-indexes.html?utm_source=chatgpt.com "18: 12.9. Preferred Index Types for Text Search"
[3]: https://fastapi.tiangolo.com/advanced/custom-response/?utm_source=chatgpt.com "Custom Response - HTML, Stream, File, others - FastAPI"
